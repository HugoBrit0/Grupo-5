@model WebApplication1.Models.AdViewModel

@{
    ViewData["Title"] = "Criar Anúncio";
}

<h2>Criar Anúncio</h2>

<form asp-action="Create" method="post" enctype="multipart/form-data" class="mt-4">
    <div class="form-group">
        <label for="Marca">Marca</label>
        <select asp-for="Marca" class="form-control">
            <option value="Audi">Audi</option>
            <option value="BMW">BMW</option>
            <option value="Mercedes">Mercedes</option>
            <option value="Volkswagen">Volkswagen</option>
        </select>
    </div>
    <div class="form-group">
        <label for="Quilometragem">Quilometragem</label>
        <input type="number" asp-for="Quilometragem" class="form-control" required />
    </div>
    <div class="form-group">
        <label for="Ano">Ano</label>
        <input type="number" asp-for="Ano" class="form-control" required />
    </div>
    <div class="form-group">
        <label for="Estado">Estado</label>
        <select asp-for="Estado" class="form-control">
            <option value="Novo">Novo</option>
            <option value="Usado">Usado</option>
            <option value="Semi-novo">Semi-novo</option>
        </select>
    </div>
    <div class="form-group">
        <label for="imageFile">Imagem do Carro</label>
        <input type="file" name="imageFile" class="form-control-file" />
    </div>
    <button type="submit" class="btn btn-primary">Criar Anúncio</button>
</form>

<h2>Anúncios Disponíveis</h2>

<div class="container mt-4">
    <div class="row">
        @if (ViewData["Ads"] is List<WebApplication1.Models.AdViewModel> ads)
        {
            foreach (var ad in ads)
            {
                <div class="col-md-4 mb-4">
                    <div class="card">
                        @if (!string.IsNullOrEmpty(ad.ImagePath))
                        {
                            <img src="@ad.ImagePath" class="card-img-top" alt="Imagem do Anúncio" style="max-height: 200px; object-fit: cover;" />
                        }
                        <div class="card-body">
                            <h5 class="card-title">Marca: @ad.Marca</h5>
                            <p class="card-text">Quilometragem: @ad.Quilometragem km</p>
                            <p class="card-text">Ano: @ad.Ano</p>
                            <p class="card-text">Estado: @ad.Estado</p>
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div>
